"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _npmlog = _interopRequireDefault(require("npmlog"));

var _ = require(".");

jest.mock('npmlog', function () {
  return {
    info: jest.fn(),
    warn: jest.fn(),
    error: jest.fn()
  };
});
describe('node-logger', function () {
  beforeEach(function () {
    _npmlog.default.info.mockReset();

    _npmlog.default.warn.mockReset();

    _npmlog.default.error.mockReset();
  });
  it('should have an info method', function () {
    var message = 'information';

    _.logger.info(message);

    expect(_npmlog.default.info).toHaveBeenCalledWith('', message);
  });
  it('should have a warn method', function () {
    var message = 'warning message';

    _.logger.warn(message);

    expect(_npmlog.default.warn).toHaveBeenCalledWith('', message);
  });
  it('should have an error method', function () {
    var message = 'error message';

    _.logger.error(message);

    expect(_npmlog.default.error).toHaveBeenCalledWith('', message);
  });
});